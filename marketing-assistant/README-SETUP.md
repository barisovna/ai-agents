# AI Маркетинг Ассистент Pro - Инструкция по настройке

## 📋 Быстрая настройка

### 1. Создайте файл `config.php`
```php
<?php
return [
    'deepseek_key' => 'sk-ваш-ключ-здесь'
];
?>
```

Получить ключ: https://platform.deepseek.com/api_keys

### 2. Загрузите файлы на сервер
- `marketing-assistant-improved.html` - главный файл
- `api-proxy.php` - прокси для API
- `config.php` - ваш API ключ

### 3. Откройте в браузере
Перейдите на: `https://ваш-домен.ru/marketing-assistant-improved.html`

---

## 🔧 Модели DeepSeek

### deepseek-chat (По умолчанию)
- **Скорость:** Быстро
- **Макс токенов:** 8,192
- **Цена:** $0.28 / 1M input токенов
- **Подходит для:** Обычные маркетинговые задачи, быстрые ответы

### deepseek-reasoner (Продвинутый)
- **Скорость:** Медленнее
- **Макс токенов:** 65,536
- **Цена:** Та же
- **Подходит для:** Сложный анализ, стратегическое планирование

---

## 🐛 Отладка ошибок

### Ошибка: "Connection error"

**Причины:**
1. Неверный API ключ в `config.php`
2. Блокировка файрволом
3. Проблемы с интернетом на сервере
4. CURL не настроен на сервере

**Проверка:**
```bash
# Проверьте логи
cat api-debug.log
cat api-errors.log

# Тест CURL
curl -I https://api.deepseek.com/v1/chat/completions
```

### Ошибка: "Config file not found"

**Решение:**
```bash
# Убедитесь что config.php в той же папке что и api-proxy.php
ls -la config.php

# Проверьте права доступа
chmod 644 config.php
```

### Ошибка: "Invalid JSON input"

**Причина:** Проблема с кодировкой или форматом данных

**Решение:**
1. Проверьте консоль браузера (F12)
2. Убедитесь что файлы в UTF-8
3. Проверьте `api-debug.log`

---

## 📊 Файлы логов

Система автоматически создает логи:

### `api-debug.log`
Запросы к API:
```json
{
  "timestamp": "2025-01-15 10:30:45",
  "model": "deepseek-chat",
  "max_tokens": 4096,
  "message_count": 5,
  "endpoint": "https://api.deepseek.com/v1/chat/completions"
}
```

### `api-errors.log`
Ошибки CURL или API:
```json
{
  "timestamp": "2025-01-15 10:31:12",
  "error_type": "curl_error",
  "curl_errno": 6,
  "curl_error": "Could not resolve host",
  "api_url": "https://api.deepseek.com/v1/chat/completions"
}
```

**Чтобы отключить логи** (в продакшене):
Закомментируйте в `api-proxy.php`:
```php
// file_put_contents(__DIR__ . '/api-debug.log', json_encode($logData) . "\n", FILE_APPEND);
```

---

## ⚙️ Требования сервера

- **PHP:** 7.4 или выше
- **CURL:** Включен
- **OpenSSL:** Для HTTPS
- **Память:** 256MB минимум
- **Тайм-аут:** 180 секунд

**Проверка:**
```php
<?php
phpinfo();
```

Найдите:
- `allow_url_fopen` = On
- `curl` = Enabled
- `openssl` = Enabled

---

## 🚀 Оптимизация производительности

### 1. Выбор правильной модели
- Для коротких ответов: `deepseek-chat`
- Для анализа: `deepseek-reasoner`

### 2. Оптимизация токенов
```javascript
// Короткие ответы - меньше токенов
max_tokens: 2048

// Длинные стратегии - больше токенов
max_tokens: 4096
```

### 3. Управление историей
История чата ограничена 20 сообщениями:
```javascript
if (conversationHistory.length > 20) {
    conversationHistory = conversationHistory.slice(-20);
}
```

---

## 💰 Расчет стоимости

**Пример:**
- 1000 запросов
- Средний ввод: 500 токенов
- Средний вывод: 2000 токенов

**Расчет:**
```
Input:  1000 × 500 = 500,000 токенов = $0.14
Output: 1000 × 2000 = 2,000,000 токенов = $0.84
Итого: $0.98 за 1000 запросов
```

---

## 🔒 Безопасность

### Защита API ключа
```php
// config.php должен быть вне веб-корня
// ИЛИ защищен .htaccess:

<Files "config.php">
    Order allow,deny
    Deny from all
</Files>
```

### Ограничение запросов
Добавьте rate limiting в `api-proxy.php`:
```php
// Пример: макс 60 запросов в минуту
session_start();
if (!isset($_SESSION['requests'])) $_SESSION['requests'] = [];
$_SESSION['requests'][] = time();
$_SESSION['requests'] = array_filter($_SESSION['requests'], fn($t) => $t > time() - 60);
if (count($_SESSION['requests']) > 60) {
    die(json_encode(['error' => 'Rate limit exceeded']));
}
```

---

## 📞 Поддержка

**Проблемы с API:** https://api-docs.deepseek.com/
**Баги интерфейса:** Проверьте консоль браузера (F12)
**Вопросы:** Смотрите логи `api-errors.log`

---

## 📚 Полезные ссылки

- [Документация DeepSeek API](https://api-docs.deepseek.com/)
- [Pricing](https://api-docs.deepseek.com/quick_start/pricing)
- [Лимиты и квоты](https://api-docs.deepseek.com/api_limits)
